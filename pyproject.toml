[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "coresignal"
version = "0.1.0"
description = "Computational framework for coupled inner-core, Earth-rotation, and geomagnetic research."
requires-python = ">=3.11"
