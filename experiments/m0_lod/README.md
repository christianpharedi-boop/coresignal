# M0 LOD Baseline

M0 measures out-of-sample predictability of LOD using Earth-rotation history alone.

It excludes inner-core and geomagnetic variables and requires persistence as the mandatory comparator.
