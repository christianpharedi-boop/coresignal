experiment_id: geomagnetic_m0_baseline_v001

target:
  variable: declared_before_execution
  unit: declared_before_execution

predictors:
  - baseline_history

model:
  family: baseline

validation:
  method: rolling_origin
  final_test_locked: true

primary_metric:
  name: declared_before_execution
  direction: declared_before_execution

acceptance:
  threshold_declared_before_final_test: true
