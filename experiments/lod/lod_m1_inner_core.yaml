experiment_id: lod_m1_inner_core_v001

target:
  variable: dLOD
  unit: ms

predictors:
  - baseline_history
  - DeltaOmega_IC

model:
  family: inner_core_augmented
  lag: fixed_or_training_selected

validation:
  method: rolling_origin
  final_test_locked: true

primary_metric:
  name: RMSE
  direction: lower_is_better

acceptance:
  threshold_declared_before_final_test: true
