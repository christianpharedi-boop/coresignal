experiment_id: lod_m0_baseline_v001

target:
  variable: dLOD
  unit: ms

predictors:
  - baseline_history

model:
  family: baseline
  lag: none

validation:
  method: rolling_origin
  final_test_locked: true

primary_metric:
  name: RMSE
  direction: lower_is_better

acceptance:
  threshold_declared_before_final_test: true
