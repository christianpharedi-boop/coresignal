experiment_id: lod_m0_baseline_v001
status: superseded
canonical_manifest: false
superseded_by: experiments/m0_lod/m0_manifest.yaml
notes: Historical compatibility record retained for provenance; do not execute as the canonical M0 specification.

target:
  variable: dLOD
  unit: ms

predictors:
  - baseline_history

model:
  family: baseline
  lag: none

validation:
  method: fixed_temporal_holdout
  final_test_locked: true

primary_metric:
  name: RMSE
  direction: lower_is_better

acceptance:
  threshold_declared_before_final_test: true
